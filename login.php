<?php require 'db.php'; session_start(); if ($_SERVER["REQUEST_METHOD"] == "POST") { $login = $conn->real_escape_string($_POST['login']); $password = $_POST['password']; $sql = "SELECT * FROM users WHERE username='$login' OR email='$login' OR phone='$login' LIMIT 1"; $result = $conn->query($sql); if ($result->num_rows === 1) { $user = $result->fetch_assoc(); if (!password_verify($password, $user['password'])) { echo "❌ මුරපදය වැරදියි."; } elseif (!$user['is_verified']) { echo "⚠️ කරුණාකර ඔබේ ගිණුම verify කරන්න. <a href='verify.php'>සත්‍යාපනය</a>"; } else { $_SESSION['user_id'] = $user['id']; $_SESSION['username'] = $user['username']; echo "✅ Login සාර්ථකයි! 🎉"; echo "<br><a href='dashboard.php'>Dashboard එකට යන්න</a>"; } } else { echo "❌ ගිණුමක් හමු නොවුණි."; } } ?> <!-- HTML Form --> <!DOCTYPE html> <html lang="si"> <head> <meta charset="UTF-8"> <title>Login</title> <link rel="stylesheet" href="assets/style.css"> </head> <body> <h2>පිවිසෙන්න</h2> <form method="POST"> <input type="text" name="login" placeholder="Username / Email / Phone" required><br> <input type="password" name="password" placeholder="Password" required><br> <button type="submit">Login</button> </form> <p><a href="forgot_password.php">මුරපදය අමතකද?</a></p> </body> </html>
